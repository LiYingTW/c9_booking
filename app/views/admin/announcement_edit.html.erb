<%= form_for @announcement, :url => { :controller => 'admin', :action => 'announcement_update' } do | f | %>
    <%= render :partial => 'announcement_form', :locals => { :f => f } %>
    <div>
        <%= f.submit "update" %>
        <%= f.hidden_field :id %>
    </div>
<% end %>